using UnityEngine;

public class MenuAnimations : MonoBehaviour
{
    /*public void OpenAnimation(GameObject menu)
    {
        Vector2 originalScale = menu.transform.localScale;
        menu.transform.localScale = Vector2.zero;
        OpenMenu(menu);
        menu.transform.LeanScale(originalScale, 0.2f).setEaseInQuint();
    }

    public void CloseAnimation(GameObject menu)
    {
        menu.transform.LeanScale(Vector2.zero, 0.2f).setEaseInBack();
        CloseMenu(menu);
    }*/
}
